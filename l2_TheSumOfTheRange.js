let range = (start, end, step) => {
    let numbers = [];
    for (let i = start; i <= end; i += step){
        numbers.push(i);
    }
    return numbers;
}

let sum = (number) => {
   for(let i = 0; i <= number.length; i++){
         
   }
}

